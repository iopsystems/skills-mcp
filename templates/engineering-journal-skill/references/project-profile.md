# Engineering Journal Project Profile

Fill this file from observed repository conventions before using the installed
skill. Cite the source for each choice. Replace every placeholder; do not infer a
policy merely because a similarly named file exists.

## Repository Convention

- Project name: `<name>`
- Journal path and filename pattern: `<path/pattern>`
- Index path: `<path>`
- Index shape and required columns: `<description>`
- Repository guidance sources: `<paths>`

## Entry Schema

- Required frontmatter: `<fields>`
- Optional frontmatter: `<fields and when allowed>`
- Required headings: `<headings>`
- Fixed lifecycle states: `open`, `shipped`, `no-go`, `superseded`
- Lifecycle extensions: `<extra metadata or stricter transition rules, never additional status values>`
- Date and supersession rules: `<rules>`

## Operating Policy

- Operating-mode preference: `<intent-first, single-PR, or decide per effort>`
- Intent-first landing requirement: `<policy>`
- Trivial-change threshold: `<policy>`
- Evidence requirements: `<paths, commits, measurements, commands, or other>`

## Durable Derived Documents

Record only documents this project already treats as durable or has explicitly
approved. Absence is a valid answer.

- Backlog: `<path or none>`
- Roadmap: `<path or none>`
- Assumptions and limitations: `<path or none>`
- Canonical facts, datasets, bibliographies, or other records: `<paths or none>`
- Derived-document policy: `<when each document must be synchronized>`

Do not create a project-wide backlog by default. Add one only after explicit
project approval.

## Validation and Reconciliation

- Validation commands: `<commands>`
- Link or index checks: `<commands or manual checks>`
- Reconciliation boundaries: `<paths and records in scope>`
- Judgment changes requiring review: `<categories>`
- Advisory brief reporting policy: `<policy>`

## Profile Evidence

- Filled by and date: `<identity/date>`
- Evidence inspected: `<paths and commits>`
- Unresolved conflicts or unknowns: `<none or details>`
